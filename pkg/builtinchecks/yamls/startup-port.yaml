name: "startup-port"
description: "Indicates when containers have a startup probe to a not exposed port."
remediation: >-
  Check which ports you've exposed and ensure they match what you have specified
  in the startup probe.
scope:
  objectKinds:
    - DeploymentLike
template: "startup-port"
